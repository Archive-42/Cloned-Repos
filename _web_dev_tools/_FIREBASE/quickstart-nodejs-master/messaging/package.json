{
  "name": "messaging",
  "scripts": {
    "compile": "cp ../tsconfig.json.template ./tsconfig.json && tsc"
  },
  "dependencies": {
    "google-auth-library": "^6.0.3",
    "googleapis": "^171.4.0"
  }
}
